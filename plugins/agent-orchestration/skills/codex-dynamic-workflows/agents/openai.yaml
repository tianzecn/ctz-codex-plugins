interface:
  display_name: AI Agent 动态工作流
  short_description: 规划并运行复杂任务的多 Agent 编排、审批与验证工作流。
  default_prompt: Use $codex-dynamic-workflows to plan and run a supervised multi-agent
    workflow for this task.
