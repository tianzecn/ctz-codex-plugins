# Upstream provenance

- Repository: https://github.com/virgiliojr94/book-to-skill
- Commit: `9c207f870adebe20ade4f7d2f11bc3d759c2fd88`
- Upstream package version: `1.4.0`
- Imported: 2026-08-31
- License: MIT (see `LICENSE.md`)

Imported files include the root `SKILL.md`, its deterministic extraction package,
the `scripts/extract.py` entrypoint, the generated-skill security scanner, and the
upstream Python package metadata. The Codex package adds Chinese display metadata
and the minimum Codex skill/cache discovery paths needed by the imported workflow.
Trailing whitespace and one redundant final blank line in two Python files were
normalized to satisfy the target repository's diff checks; executable logic is unchanged.
